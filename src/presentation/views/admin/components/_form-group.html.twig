<div class="form-group">
  <label for="{{ id }}">{{ label }}</label>
  <input type="{{ type|default('text') }}" class="form-control" id="{{ id }}" name="{{ name }}" placeholder="{{ placeholder }}" value="{{ value|default('') }}">
</div>
